{% extends 'base.html.twig' %}

{% block title %}
    {% if edit is defined %}
        Actualizar tarea
    {% else %}
        Crear tarea
    {% endif %}

{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h2>
            {% if edit is defined %}
                Actualizar tarea
            {% else %}
                Crear tarea
            {% endif %}
        </h2>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

    {# <button id="btnHola"> Prueba </button>
        {{ include('includes/form-contact.html.twig') }} #}

    {% block javascripts %}

    <script type="text/javascript" src=" {{ asset('assets/js/libs/jquery.js')}} ">
    </script>

    <script type="text/javascript" src=" {{ asset('assets/js/libs/push.js')}} ">
    </script>

    <script type="text/javascript" src=" {{ asset('assets/js/notification.js')}} "/>
    </script>

    {% endblock %}

{% endblock %}
